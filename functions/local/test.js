const {run_db_trigger} = require("../onCreate/db_trigger")
const {cycle_speler_serie_list, cycle_speler_years_list} = require("../pubsub/cycle/speler")

const go = async () => {

    return true
}

go()